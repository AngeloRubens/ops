# The same server as the redis scenario, on the debian base rather than the alpine one, to tell
# the server apart from the toolchain its alpine image is built with. redis is one process with
# threads, which is the shape a unikernel is made for.
FROM redis:8
