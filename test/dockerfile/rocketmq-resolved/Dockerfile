# The official RocketMQ image with the role its entrypoint wants named: the image's CMD is a
# placeholder the entrypoint runs as it is, so without a role nothing starts. The name server is the
# part that stands on its own.
FROM ops-test-rocketmq:5.3.2
CMD ["nameserver"]
