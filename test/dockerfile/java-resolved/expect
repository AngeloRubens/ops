# The same image, asked what it starts: the entrypoint script ends in an exec of the jvm.
DOCKERFILE=../java/Dockerfile
EXPECT=ok
PORT=8080
OUTPUT_MATCH="hello from java"
BUILD_FLAGS="--resolve-entrypoint"
MANIFEST_CHECK='(.Program | test("java$")) and (.Args | index("/app.jar") != null)'
