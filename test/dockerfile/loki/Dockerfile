# Loki as grafana publishes it. Its entrypoint is the binary itself, in exec form, with the
# configuration the image already carries - the shape that needs nothing asked of it.
FROM grafana/loki:3.5.0
EXPOSE 3100
