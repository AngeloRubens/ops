# The most installed dockerised application there is, and the answer to "does wordpress run" ought
# to be measured rather than guessed. Its command is apache2-foreground, a script, behind an
# entrypoint script, in front of an apache that forks its workers.
FROM wordpress:php8.3-apache
