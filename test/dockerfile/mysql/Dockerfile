# The official mysql image. It initialises its data directory the first time its entrypoint runs,
# which is a shape of its own: what the image carries is not yet a database.
FROM mysql:8.4
ENV MYSQL_ALLOW_EMPTY_PASSWORD=1
